const state={data:null,period:null,query:"",sortKey:"netProfitYoY",sortDir:-1};const el=id=>document.getElementById(id);const pct=value=>Number.isFinite(value)?`${value>0?"+":""}${value.toFixed(1)}%`:"—";const cls=value=>!Number.isFinite(value)?"":value>=0?"positive":"negative";const escapeHtml=value=>String(value??"").replace(/[&<>'"]/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[ch]));const amount=value=>Number.isFinite(value)?new Intl.NumberFormat("zh-CN",{notation:"compact",maximumFractionDigits:2}).format(value):"—";
function currentPeriod(){return state.data.periods.find(item=>item.id===state.period)||state.data.periods[0]}
function render(){const period=currentPeriod();let rows=period.industries.filter(row=>`${row.name} ${row.nameZh} ${row.parent} ${row.code}`.toLowerCase().includes(state.query));rows.sort((a,b)=>{const av=a[state.sortKey],bv=b[state.sortKey];if(state.sortKey==="name")return String(av).localeCompare(String(bv),"zh-CN")*state.sortDir;if(av==null)return 1;if(bv==null)return-1;return(av-bv)*state.sortDir});el("summary-period").textContent=period.label;el("summary-industries").textContent=period.industries.length.toLocaleString();el("summary-companies").textContent=period.summary.companies.toLocaleString();el("summary-positive").textContent=period.summary.positiveNetProfit.toLocaleString();el("summary-abnormal").textContent=period.summary.abnormalRecords.toLocaleString();el("result-count").textContent=`显示 ${rows.length} / ${period.industries.length} 个行业`;el("industry-body").innerHTML=rows.map(row=>`<tr><td><span class="industry-name">${escapeHtml(row.name)}</span><span class="industry-meta">${escapeHtml(row.parent)} · ${escapeHtml(row.code)}</span></td><td class="${cls(row.netProfitYoY)}">${pct(row.netProfitYoY)}</td><td class="${cls(row.revenueYoY)}">${pct(row.revenueYoY)}</td><td class="${cls(row.priceReturn)}">${pct(row.priceReturn)}</td><td class="count-cell">${row.positiveNetProfit}<span> / ${row.netProfitObservations}</span></td><td class="count-cell">${row.positiveRevenue}<span> / ${row.revenueObservations}</span></td><td><button class="abnormal-button" data-code="${escapeHtml(row.code)}" ${row.abnormalCompanies?"":"disabled"}>${row.abnormalCompanies}</button></td><td>${row.companies}</td></tr>`).join("")}
function showAbnormal(code){const period=currentPeriod(),row=period.industries.find(item=>item.code===code);if(!row||!row.abnormalRecords.length)return;el("dialog-title").textContent=`${row.name} · ${period.label} · ${row.abnormalRecords.length} 条记录`;el("abnormal-body").innerHTML=row.abnormalRecords.map(item=>`<tr><td>${escapeHtml(item.code)} ${escapeHtml(item.name)}</td><td>${escapeHtml(item.metric)}</td><td class="${cls(item.yoy)}">${pct(item.yoy)}</td><td>${amount(item.baseline)}</td><td>${escapeHtml(item.reason)}</td></tr>`).join("");el("abnormal-dialog").showModal()}
async function init(){el("year").textContent=new Date().getFullYear();try{const response=await fetch("data/sw-industry.json",{cache:"no-store"});if(!response.ok)throw new Error(`HTTP ${response.status}`);state.data=await response.json();state.period=state.data.periods[0].id;el("period-select").innerHTML=state.data.periods.map(item=>`<option value="${escapeHtml(item.id)}">${escapeHtml(item.label)}</option>`).join("");el("data-source").textContent=`数据更新时间：${new Date(state.data.generatedAt).toLocaleString("zh-CN")}。来源：申万研究、上市公司定期报告、乐咕乐股和东方财富，数据通过 AKShare 获取。`;el("status").hidden=true;el("dashboard").hidden=false;render()}catch(error){el("status").textContent=`行业数据加载失败（${error.message}），请稍后重试。`}}
el("period-select").addEventListener("change",event=>{state.period=event.target.value;render()});el("metric-select").addEventListener("change",event=>{state.sortKey=event.target.value;state.sortDir=-1;render()});el("search-input").addEventListener("input",event=>{state.query=event.target.value.trim().toLowerCase();render()});document.querySelectorAll("th[data-sort]").forEach(th=>th.addEventListener("click",()=>{const key=th.dataset.sort;state.sortDir=state.sortKey===key?state.sortDir*-1:key==="name"?1:-1;state.sortKey=key;render()}));el("industry-body").addEventListener("click",event=>{const button=event.target.closest(".abnormal-button");if(button)showAbnormal(button.dataset.code)});el("dialog-close").addEventListener("click",()=>el("abnormal-dialog").close());init();
